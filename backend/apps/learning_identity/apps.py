from django.apps import AppConfig


class LearningIdentityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.learning_identity"
    verbose_name = "Learning Identity"

